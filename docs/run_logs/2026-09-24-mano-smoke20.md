# MANO 旋转与 DAPG 短训练记录

DexYCB Subject 01: `20200709_150949`，相机 `840412060917`，右手抓 `025_mug`。

## MANO 验收

- 模型：共享盘中的 `MANO_RIGHT.pkl`；手形取该受试者 `mano.yml` 的 10 个 betas。
- `pose_m[:48]` 为全局旋转和 45 个 PCA 系数，`pose_m[48:51]` 为米制平移。
- `pose_m` 的定义按 [DexYCB 官方工具文档](https://github.com/NVlabs/dex-ycb-toolkit#loading-dataset-and-visualizing-samples)。
- 71 个有效帧的关节标签重建误差：平均 `0.000055 mm`，最大 `0.000140 mm`。
- MANO 框架与旧几何框架的位置差：平均 `0.000045 mm`，最大 `0.000126 mm`。
- 原始旋转差：平均 `166.04°`，95 分位 `179.46°`；去掉首帧固定轴偏移后的运动差：平均 `66.53°`，95 分位 `177.15°`。两种框架的轴定义和动态均不可互换。
- 新输出：`hand_pose_mano/`、`mano_comparison.json`、`retargeting_mano.pkl`。
- Retarget 共 74 帧，首 3 帧按显式 `nearest` 策略补齐；与旧版动作的平均绝对差约 `0.514 rad`。

MANO 关节位置与标签几乎完全一致，旋转来自官方模型运动学树；验收通过。
离屏图像抽查显示手接近杯子并在后段随杯子抬起。

## 示范与训练

- MANO 示范：`data/demonstrations/relocate-mug-mano-real.pkl`，292 步，39 维观测、30 维动作，动作饱和比例 `20.76%`；验证通过。
- 配置：`configs/dapg-mug-mano-smoke.yaml`，BC 初始化和 DAPG 启用，20 次迭代，每轮 5 条采样轨迹、2 条评估轨迹、2 个 CPU 工作进程。
- 机器没有可用 CUDA；使用 `scripts/15_train_dapg_cpu.py` 令上游价值函数在 CPU 运行。
- 20 次迭代全部完成，未出现 NaN 或形状错误；`best_policy.pickle` 可以加载，3,356 个参数均有限。
- 迭代 0 的采样 / 评估回报为 `-14.01 / -20.21`；迭代 19 为 `25.54 / 34.78`。样本少，分数波动大，不能据此宣称抓取成功。
- 独立确定性回放 400 步：总奖励 `-4.60`，手杯接触 0 步，杯子最高 `0.0403 m`；这次没有抓住或抬起杯子。

训练输出在 `training_log/dapg_relocate-mug-0.8_relocate-mug-mano-real_0.1_100_mano_smoke20_seed200/`，回放图像与统计在 `data/processed/seq_dexycb_001/policy_smoke20/`。原始数据、示范和策略均不上传 GitHub。

## 下一步

先比较示范与环境重置时的手杯初始位置及坐标范围，再扩充多条经 MANO 验收的轨迹；使用独立评估统计接触和抬杯成功率。
